# Repository Guidelines

## プロジェクト構成と参照ドキュメント
- ルートの `README.md` を最初に確認し、セットアップ手順とドキュメントの読み順を把握する。
- 現時点はドキュメント中心。全体要件は `docs/requirements.md` を必ず確認し、実装時のソースオブトゥルースとする。設計変更時は必ずここを更新してからコードに反映する。
- 基本設計の全体像は `docs/basic_design.md` を必ず確認し、設計に変更がある場合は同ファイルを更新してから詳細設計・コードへ反映する。
- 設計書は `docs/design/` に整理済み（例: `strategy_core.md`, `indicator_engine.md`, `risk_manager.md`, `config_schema.md`, `backtester.md`, `runner.md`）。実装前に担当モジュールの設計を読み、前提・入出力・エッジケースを把握する。
- 取引所仕様・署名・レートリミットなど Hyperliquid 固有情報は `docs/hyperliquid/` 以下に集約。API 実装時はエンドポイント・ノンス・リミットを必ず突き合わせる。
- コードを追加する場合は `src/`（アプリ本体）、`tests/`（自動テスト）、`scripts/`（ユーティリティ）に整理し、ディレクトリ単位で責務を分ける。
- データサンプルやバックテスト用データは `data/`（機微情報は除外）に集約する想定。

## 作業開始前の必須確認
- `README.md` `docs/requirements.md` `docs/basic_design.md` を必ず読むこと。
- 実装前に `docs/tasks.md` を確認し、担当タスクと依存関係を把握する。

## 開発セットアップ・ビルド/テスト実行
- 想定ランタイムは Python 3.11+。新規着手時に仮想環境を作成: `python -m venv .venv && source .venv/bin/activate`。初回は `python -m pip install -U pip` で最新版にしておく。
- 依存管理ファイルが未作成の場合、実装と同時に `requirements.txt` または `poetry.lock` を追加し、`pip install -r requirements.txt` で再現可能にする。ライブラリを追加したら理由を PR に記載する。
- 推奨コマンド（導入後）は `pytest -q`（テスト）、`ruff check .`（静的解析）、`black .`（整形）。Makefile や `pyproject.toml` を追加する場合はコマンドのエイリアスを定義し、README に反映する。
- バックテストやデータダウンロード用のスクリプトは `scripts/` に置き、引数例と想定出力をコメントまたは README に記載する。
- ログ・スケジューラは基本 UTC で運用し、時刻の扱いを曖昧にしない。

## コーディングスタイルと命名
- PEP8 準拠、インデントはスペース 4。関数・メソッドは `snake_case`、クラスは `PascalCase`、定数は全大文字で表現し、意味のある名前を付ける。
- 型ヒントを必須とし、外部 API 応答などは `TypedDict`/`pydantic` 等で構造を明示。エラークラスは用途別に分け、例外メッセージはユーザーにわかる文章にする。
- フォーマッタは `black`、リンタは `ruff`（または `flake8`）を推奨。導入したら `pre-commit` に設定し、`pre-commit run --all-files` をプッシュ前チェックとして徹底する。
- ログは情報価値を意識し、トレード ID、シンボル、タイムフレーム、価格など復元に必要なコンテキストを含める。秘密情報は必ずマスクする。

## テスト指針
- 正常系だけでなく、レンジ/トレンド判定の境界、VWAP 乖離しきい値、連敗ストップやクールダウンの状態遷移をテーブルテストで網羅する。
- 偽の API クライアントやサンプル板情報を使ったユニットテストを優先し、バックテストは再現可能な固定データで行う。リトライやタイムアウトなど失敗系も必ずカバー。
- PR では最低限「新規テストの追加」または「既存テストが通ること」を確認し、`pytest -q` と `pre-commit` の結果を記載する。カバレッジ閾値を設定した場合はバッジ更新も検討。

## 作業フローとレビュー
- 着手前に関連ドキュメントを読み、Issue/タスクに対する確認事項（前提、入出力、失敗時挙動）を短くまとめてから実装を始める。
- 実装→Lint/Formatter→ユニットテスト→バックテスト（必要時）の順で進め、各ステップの結果を PR 説明に記録する。未対応の残件は TODO として明示する。
- レビューコメントは 24 時間以内の返答を目安とし、オーナーシップを持ってフォローアップする。大きな設計変更は別 Issue/ADR で合意形成してから実装する。
- 不明点・ブロッカーは曖昧なまま実装しない。必ずユーザー（依頼者）に確認した上で進める。
- タスク完了時は `docs/tasks.md` の該当チェックボックスを更新する。

## セキュリティと設定
- API キーや署名に必要な秘密情報は `.env` に保存し、リポジトリにはコミットしない。最小権限の API ウォレットを使用し、ログ出力前にセンシティブ情報をマスクする。
- ネットワーク呼び出しはタイムアウト・リトライ（指数バックオフ）・サーキットブレーカを考慮し、レートリミットは `docs/hyperliquid/` の値に従う。例外は握りつぶさず、ユーザーに伝わるメッセージで失敗理由を返す。
- 設定ファイルは `.env` + `config/`（想定）にまとめ、デフォルト値と本番値を分離する。秘密情報を含むサンプルは `env.example` に抜粋する。

## コミットとプルリクエスト
- コミットはコンベンショナルコミット（例: `feat: add range regime detector`, `fix: handle hyperliquid nonce error`, `chore: add hyperliquid docs link`）で小さくまとめる。1 コミット 1 トピックを徹底。
- PR では概要、動機、主要変更点、テスト結果（`pytest`/`pre-commit` のログ）、影響範囲、既知の制約を記載。仕様差分がある場合は該当ドキュメントを同一 PR で更新し、スクリーンショットやログがある場合は添付する。
- レビューで指摘された事項は設計書へのフィードバックも検討し、再発防止策があればコメントに明記する。
- ブランチ名は `feature/<topic>` `fix/<issue-id>` など目的がわかる形にし、ドラフト PR を早めに出して方向性を共有する。
